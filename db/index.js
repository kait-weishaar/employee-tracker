//function to update

const db = require("./connection");


//MODULARIZE code in future to have one funtion for each atcion for each table -12 functions total

// function findRole (table, columns, values, params) {
//     const sql = `SELECT FROM role ${columns} VALUES ${values}`;
//     db.query(sql, params, (err, res))
// }



//function to find/select

//function to delete